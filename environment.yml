name: openfe-benchmarks
channels:
  - conda-forge
  - openeye
dependencies:
  - gufe
  - openfe
  - openff-nagl-models >=0.3.0
  # testing
  - nbval
  - pytest
  - pytest-xdist
  - pytest-cov
  - coverage
  - openeye-toolkits
  # not sure why we don't install from conda-forge
  - pip
  - pip:
      - py3dmol
