# Adding Partial Charge Files

The provided `conda_lock_linux-64.yml` has been generated for a `linux-64` machine. Future generation of future partial charges should also be run on the same architecture.

